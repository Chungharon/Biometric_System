# ─────────────────────────────────────────────────────────────────────────────
#  Biometric Authentication System – Python dependencies
#  Install with:  pip install -r requirements.txt
#
#  NOTE – face_recognition requires dlib which requires cmake:
#    macOS  : brew install cmake
#    Ubuntu : sudo apt-get install -y cmake build-essential
#    Windows: install cmake from https://cmake.org/download/
#  Then run:  pip install dlib face_recognition
# ─────────────────────────────────────────────────────────────────────────────

# ── Core vision & face recognition ───────────────────────────────────────────
opencv-python==4.9.0.80          # webcam capture, image processing
face_recognition==1.3.0          # 128-d face encodings (requires dlib)
dlib==19.24.2                    # C++ ML library underpinning face_recognition
numpy==1.26.4                    # numerical arrays

# ── Security ─────────────────────────────────────────────────────────────────
bcrypt==4.1.3                    # PIN / password hashing

# ── Utilities ────────────────────────────────────────────────────────────────
Pillow==10.3.0                   # optional image I/O (enrollment preview saves)

# ── Standard-library modules used (no install needed) ────────────────────────
# sqlite3, csv, os, sys, pickle, datetime, uuid, getpass, contextlib, typing
